import 'package:flutter/material.dart';

class ThemeTokens {
  // Cores Primárias
  static const Color primaryColor = Color(0xFF00BCD4);
  static const Color primaryColorLight = Color(0xFFB2EBF2);
  static const Color primaryColorDark = Color(0xFF008BA3);

  // Cores Secundárias
  static const Color secondaryColor = Color(0xFFFFFFFF);
  static const Color secondaryColorDark = Color(0xFFE0E0E0);

  // Cor de Destaque
  static const Color highlightColor = Color(0xFF4CAF50);
  static const Color highlightColorLight = Color(0xFFC8E6C9);
  static const Color highlightColorDark = Color(0xFF388E3C);

  // Cores de Fundo
  static const Color backgroundColor = Color(0xFFECEFF1);
  static const Color backgroundLightColor = Color(0xFFFFFFFF);
  static const Color backgroundDarkColor = Color(0xFFCFD8DC);

  // Cores de Texto
  static const Color textPrimaryColor = Color(0xFF212121);
  static const Color textSecondaryColor = Color(0xFF757575);

  // Espaçamento
  static const double paddingSmall = 8.0;
  static const double paddingMedium = 16.0;
  static const double paddingLarge = 24.0;

  // Raio de Borda
  static const double borderRadiusSmall = 4.0;
  static const double borderRadiusMedium = 8.0;

  // Tamanhos de Texto
  static const double fontSizeTitle = 24.0;
  static const double fontSizeBody = 16.0;
}